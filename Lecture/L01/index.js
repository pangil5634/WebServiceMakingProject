const screenWidth = window.innerWidth;
const screenHeight = window.innerHeight;

const element = document.body; // 해당 요소의 ID를 가져옴
element.style.width = screenWidth + "px";
element.style.height = screenHeight+ "px";